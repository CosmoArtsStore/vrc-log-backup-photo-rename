# AITools

AI が生成した調査用スクリプト、分析メモ、抽出結果はこの配下で管理する。

## 配置方針

- 実行可能な補助スクリプトは `scripts/`
- 作業メモや調査依頼は `docs/`
- 実行結果や抽出物は `output/`

## 運用ルール

- ルート直下には新しい AI 調査物を置かない
- 再生成可能な出力は `output/` に寄せ、必要に応じて `.gitignore` で除外する
- 継続利用するスクリプトは用途が分かる名前にする
