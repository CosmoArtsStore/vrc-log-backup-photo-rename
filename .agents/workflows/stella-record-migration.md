---
description: StellaRecordのディレクトリ移行と初期セットアップ自動化
---

// turbo-all

1. `StellaRecord` の新しいディレクトリ構造を作成する
2. `LogBackupTool` 配下の旧プロジェクトを新構成の `Polaris` や `stella_core` にコピー/移動する
3. `tmp/vrc_log_parser` を `PLANETARIUM` にコピー/移動する
4. `PhotoRenameApp` を `Atlas` にコピー/移動する
5. 新規 `PLEIADES` のTauriプロジェクトの土台を作成する
6. 古いフォルダや不要な一時ファイルをクリーンアップする
7. その他の各種ファイル修正タスクを順次実行する
