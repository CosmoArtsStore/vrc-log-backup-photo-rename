# Vercel / ローカル開発で共通の変数名。コピーして .env.local（Next）または .env（desktop）で使用。
# Next: cp .env.example .env.local
# desktop: desktop フォルダで cp .env.example .env

# 管理画面ログイン用パスワード（必須）
CHOCOMELAPP_ADMIN_PASSWORD=

# Google Sheets（任意。Next は credentials.json または GOOGLE_CREDENTIALS を参照）
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\credentials.json
