# Alpheratz 実装状況整理

更新日: 2026-03-13

## 実装済み

- フォルダ指定
- 手動スキャン
- メモ保存
- ワールド名検索
- ワールドプルダウン検索
- 日付フィルタ
- 縦横向きフィルタ
- 類似画像検索
- VRChat ワールドリンク
- 条件検索サイドバー
- タグ UI
- お気に入り UI

## 仮実装

### タグ
- 現在はフロントエンド state 上のみで保持
- セッションをまたいで保存されない
- DB 永続化は未実装

### お気に入り
- 現在はフロントエンド state 上のみで保持
- セッションをまたいで保存されない
- DB 永続化は未実装

### 縦横向き判定
- 現在はファイル名中の `WIDTHxHEIGHT` を使った仮推定
- DB 保存された `width` / `height` / `orientation` を正式に使う実装ではない

## 未実装

### ヒストグラム関連
- `histogram` 計算
- `histogram` 保存
- 色フィルター検索

### 差分同期
- フォルダに存在しない写真の DB からの物理削除
- 差分削除同期の明示実装

### 同一写真照合
- ファイル名照合
- メタデータ照合
- pHash を最終手段とする同一確定ロジック
- pHash 推測時の `?` 表示

### DB / モデル反映
- `is_favorite` の backend 反映
- `tags` の backend 反映
- `width` / `height` / `orientation` の正式保存

## 構造面の未整理

- DB スキーマ定義の一本化
- `add_column_if_missing` の堅牢化
- `COUNT(*)` を使った capacity 見積もりの整理

## 現時点の優先候補

1. タグ / お気に入りの永続化
2. ヒストグラム計算と色フィルター
3. 差分削除同期
4. 同一写真照合の明確化
