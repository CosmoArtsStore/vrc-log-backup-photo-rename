# 開発用: 必要ならこのファイルをコピーして .env を作り、値を設定してください。
# 例: cp .env.example .env
# デスクトップ版ではログインは使わないため、パスワードの設定は不要です。

# Google Sheets 用（任意。未設定の場合は desktop フォルダの credentials.json を参照）
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\credentials.json
