{
    "name": "stella-record",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "Polaris/polaris_ui",
        "Atlas",
        "PLEIADES"
    ],
    "scripts": {
        "build:Polaris-pkg": "npm run build:Polaris-bin && cd Polaris && npx tauri build",
        "build:Polaris-bin": "cd Polaris && set CARGO_TARGET_DIR=target&& cargo build --release -q",
        "build:Planetarium-bin": "cargo build --release -p planetarium -q",
        "build:StellaRecord-pkg": "npm run build:Planetarium-bin && cd stella_record_ui && npx tauri build",
        "dev:StellaRecord": "cd stella_record_ui && npx tauri dev",
        "build:Alpheratz-pkg": "cd pleiades_alpheratz && npx tauri build"
    }
}