{
    "name": "stella-record",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "Polaris/polaris_ui",
        "Atlas",
        "PLEIADES"
    ],
    "scripts": {
        "build:Polaris-pkg": "npm run build:Polaris-bin && cd Polaris && set TAURI_BUNDLE_TARGET_DIR=F:\\DEVELOPFOLDER\\RE-NAME-SYS\\StellaRecord\\target&& npx tauri build",
        "build:Polaris-bin": "cargo build --release -p polaris -q",
        "build:Planetarium-bin": "cargo build --release -p planetarium -q",
        "build:StellaRecord-pkg": "npm run build:Planetarium-bin && cd stella_record_ui && set TAURI_BUNDLE_TARGET_DIR=F:\\DEVELOPFOLDER\\RE-NAME-SYS\\StellaRecord\\target&& npx tauri build",
        "dev:StellaRecord": "cd stella_record_ui && npx tauri dev",
        "build:Alpheratz-pkg": "cd pleiades_alpheratz && set TAURI_BUNDLE_TARGET_DIR=F:\\DEVELOPFOLDER\\RE-NAME-SYS\\StellaRecord\\target&& npx tauri build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.10.0"
    }
}